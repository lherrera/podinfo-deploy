apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: traefik
  name: metrics-demo
  namespace: dev
spec:
  rules:
  - http:
      paths:
      - backend:
          serviceName: metrics-demo
          servicePort: 3000
